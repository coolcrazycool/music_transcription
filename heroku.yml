build:
  docker:
    web: Dockerfile
    nginx: ./nginx/Dockerfile
run:
  web: gunicorn -w 4 hyper_music.wsgi:application -b :$PORT
  nginx:
    command:
      - pip install docker-compose
      - docker-compose -f docker-compose.yml up -d --build
      - docker-compose -f docker-compose.yml up
